/*
1. Faça um algoritmo que apresente um menu para o usuário decidir se quer
exercitar fatos sobre (1) biologia ou (2) geografia.
◦ Se o usuário escolher a primeira opção, apresentar um menu com as
opções: (1) Exercitar sobre artrópodes ou (2) sobre vertebrados.
▪ Se o usuário escolher 1, o algoritmo deve perguntar: quantas patas
tem um inseto? Se o usuário responder 6, o algoritmo deve
apresentar a mensagem “Correto”. Do contrário, o algoritmo deve
apresentar a mensagem “Errado” e pedir para tentar de novo.
▪ Se o usuário escolher 2, o algoritmo deve perguntar: qual destes
animais não pertence aos vertebrados (1-Mamífero; 2-Réptil; 3-Ave;
4-Molusco). Se o usuário responder 4, o algoritmo deve apresentar
a mensagem “Correto”. Do contrário, o algoritmo deve apresentar
a mensagem “Errado” e pedir para tentar de novo.
◦ Se o usuário escolher a segunda opção do primeiro menu, um novo
menu com as opções: (1) Exercitar sobre países ou (2) exercitar sobre
continentes;
▪ Se o usuário escolher 1, o algoritmo deve perguntar: qual destes
países não faz fronteira com o Brasil: (1) Colômbia; (2) Chile; (3)
Venezuela. Se o usuário responder 2, o algoritmo deve apresentar
a mensagem “Correto”. Do contrário, o algoritmo deve apresentar
a mensagem “Errado” e pedir para tentar de novo.
▪ Se o usuário escolher 2, o algoritmo deve perguntar: qual destes não
é um continente (1-Ásia; 2-África; 3-Atlântida). Se o usuário
responder 3, o algoritmo deve apresentar a mensagem “Correto”.
Do contrário, o algoritmo deve apresentar a mensagem “Errado” e
pedir para tentar de novo.
*/

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
